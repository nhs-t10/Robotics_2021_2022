#i:
positionOne(), when (liftMovementFinished()) next;
positionTwo(), when (liftMovementFinished()) next;
positionThree(), when (liftMovementFinished()) next;
positionHome(), when (liftMovementFinished()) next;
log("done!");